# Railway 部署配置
# 每个服务单独部署，使用各自目录下的 Dockerfile

# Agent Service
[services.agent]
build = "./agent-service"

# Backend Service
[services.backend]
build = "./backend"

# Frontend Service
[services.frontend]
build = "./frontend"
